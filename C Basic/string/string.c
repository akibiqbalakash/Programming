#include<stdio.h>
int main(){

      char ch[]="Hello world";  //Creating a string in C requires creating an array of characters.
         printf("%s",ch);  //can be used with the specifier %s to tell C that we are now dealing with strings.

return 0;
}
